# ~/.bashrc - for interactive bash only

# Exit if not running interactively
[[ $- != *i* ]] && return

# Enable bash options
shopt -s checkwinsize # Update LINES and COLUMNS
shopt -s histappend   # Append to history
shopt -s cmdhist      # Multi-line commands as single history entry

# History control
HISTCONTROL=ignoredups:erasedups
HISTSIZE=10000
HISTFILESIZE=20000

# Keybindings
bind 'set completion-ignore-case on'
bind 'set show-all-if-ambiguous on'

# Prompt
PS1='\u@\h:\w\$ '

# Source extra bash-only configs if present
if [[ -f ~/.bash_aliases ]]; then
  source ~/.bash_aliases
fi

# ~/.bashrc
# Prompt: $bash<major>.<minor>.<patch>
PS1='\$\s'"${BASH_VERSINFO[0]}.${BASH_VERSINFO[1]}.${BASH_VERSINFO[2]} "
